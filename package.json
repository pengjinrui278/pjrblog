{
  "name": "xixi-blog",
  "type": "module",
  "version": "0.1.0",
  "description": "曦曦的博客 —— 书房 × 工坊 × 旅程",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "pagefind --site dist/client --output-path dist/client/pagefind && node -e \"const fs=require('fs'),dst='.vercel/output/static/pagefind';if(fs.existsSync('.vercel/output')){fs.rmSync(dst,{recursive:true,force:true});fs.cpSync('dist/client/pagefind',dst,{recursive:true});console.log('[postbuild] pagefind → .vercel/output/static')}else{console.log('[postbuild] no .vercel dir, skip copy')}\"",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.2.1",
    "@astrojs/vercel": "^8.0.0",
    "astro": "^5.0.0",
    "katex": "^0.16.11",
    "maplibre-gl": "^5.0.0",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
